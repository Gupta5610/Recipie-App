import { DrowpdownDirectiveDirective } from './drowpdown-directive.directive';

describe('DrowpdownDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DrowpdownDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
